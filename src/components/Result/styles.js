import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  textResult: {
    marginTop: 30,
    alignSelf: "center",
    fontSize: 25,
    color: "green"
  }
});

export default styles;